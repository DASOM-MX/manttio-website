Local SVG icons go here (astro-icon picks them up as `local:<filename>`).
Empty for now — the site uses the lucide set via `@iconify-json/lucide`.
This file also silences astro-icon's missing-directory warning at build time.
